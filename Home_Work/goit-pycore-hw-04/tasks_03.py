"""
Завдання 3
Розробіть скрипт, який приймає шлях до директорії в якості аргументу командного рядка і візуалізує структуру цієї директорії, виводячи імена всіх піддиректорій та файлів. Для кращого візуального сприйняття, імена директорій та файлів мають відрізнятися за кольором.

Вимоги до завдання:
Створіть віртуальне оточення Python для ізоляції залежностей проекту.
Скрипт має отримувати шлях до директорії як аргумент при запуску. 
Цей шлях вказує, де знаходиться директорія, структуру якої потрібно відобразити.
Використання бібліотеки colorama для реалізації кольорового виведення.
Скрипт має коректно відображати як імена директорій, так і імена файлів, використовуючи рекурсивний спосіб обходу директорій (можна, за бажанням, використати не рекурсивний спосіб).
Повинна бути перевірка та обробка помилок, наприклад, якщо вказаний шлях не існує або він не веде до директорії.

Рекомендації для виконання:
Спочатку встановіть бібліотеку colorama. 
Для цього створіть та активуйте віртуальне оточення Python, а потім встановіть пакет за допомогою pip.
Використовуйте модуль sys для отримання шляху до директорії як аргументу командного рядка.
Для роботи з файловою системою використовуйте модуль pathlib.
Забезпечте належне форматування виводу, використовуючи функції colorama.

Критерії оцінювання:
Створення та використання віртуального оточення.
Правильність отримання та обробки шляху до директорії.
Точність виведення структури директорії.
Коректне застосування кольорового виведення за допомогою colorama.
Якість коду, включаючи читабельність, структурування та коментарі.

Приклад використання:
Якщо виконати скрипт та передати йому абсолютний шлях до директорії як параметр.
python hw03.py /шлях/до/вашої/директорії

Це призведе до виведення в терміналі списку всіх піддиректорій та файлів у вказаній директорії з використанням різних кольорів для піддиректорій та файлів, що полегшить візуальне сприйняття файлової структури.

Для директорії зі наступною структурою
📦picture
 ┣ 📂Logo
 ┃ ┣ 📜IBM+Logo.png
 ┃ ┣ 📜ibm.svg
 ┃ ┗ 📜logo-tm.png
 ┣ 📜bot-icon.png
 ┗ 📜mongodb.jpg
Скрипт повинен вивести схожу структуру
"""

import sys
from pathlib import Path
from colorama import init, Fore, Style


def display_directory_tree(directory_path: Path, prefix: str=""):
    # Отримаємо вміст директорії. Обробляємо можливі помилки доступу.
    try:
        contents = sorted(list(directory_path.iterdir()), key=lambda p: p.is_file())
    except PermissionError:
        print(f"{prefix}┣━━ {Fore.RED}Помилка доступу{Style.RESET_ALL}")
        return
    except FileNotFoundError:
        print(f"{prefix}┣━━ {Fore.RED}Директорію не знайдено{Style.RESET_ALL}")
        return

    # Створимо "вказівники" для красивого відображення дерева
    pointers = ['┣━━ '] * (len(contents) - 1) + ['┗━━ ']

    for pointer, path in zip(pointers, contents):
        if path.is_dir():
            # Виведемо ім'я директорії синім кольором
            print(f"{prefix}{pointer}📂 {Fore.BLUE}{path.name}{Style.RESET_ALL}")
            # Визначио префікс для наступного рівня вкладеності
            extension = '┃   ' if pointer == '┣━━ ' else '    '
            # Рекурсивний виклик для піддиректорії
            display_directory_tree(path, prefix=prefix + extension)
        else:
            # Виведемо ім'я файлу зеленим кольором
            print(f"{prefix}{pointer}📜 {Fore.GREEN}{path.name}{Style.RESET_ALL}")


def main():
    # Ініціалізуємо colorama для роботи в терміналі Windows
    init(autoreset=True)

    # Перевіримо, чи був переданий аргумент командного рядка
    if len(sys.argv) < 2:
        print(f"{Fore.RED}Помилка: Будь ласка, вкажіть шлях до директорії.")
        print(f"Приклад: python {sys.argv[0]} /шлях/до/директорії")
        sys.exit(1)

    # Отримаємо шлях з аргументів та створимо об'єкт Path
    root_path = Path(sys.argv[1])

    # Перевіримо, чи існує шлях і чи є він директорією
    if not root_path.exists():
        print(f"{Fore.RED}Помилка: Вказаний шлях '{root_path}' не існує.")
        sys.exit(1)

    if not root_path.is_dir():
        print(f"{Fore.RED}Помилка: Вказаний шлях '{root_path}' не є директорією.")
        sys.exit(1)

    # Виведемо назву кореневої директорії та запускаємо обхід
    print(f"\n📦 {Fore.CYAN}{root_path.name}{Style.RESET_ALL}")
    display_directory_tree(root_path)


if __name__ == "__main__":
    main()
